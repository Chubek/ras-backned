require("dotenv").config({ path: appRoot + "/.env" });

/* Add any helper function that you think will be needed for you module in this file. It's recommended */
/* that you create a module named "Global" to carry any global functions, routes and modules needed. */